# Vogent Turn - Turn detection library dependencies
# Install with: pip install -r requirements.txt
# or: pip install vogent-turn

# Core ML dependencies
torch>=2.1.0
transformers>=4.35.0
numpy>=1.24.0

# Hugging Face Hub (for model downloading)
huggingface-hub>=0.19.0

# Audio processing
soundfile>=0.12.0          # For loading audio files  
librosa>=0.10.0            # For resampling
resampy>=0.4.2             # Required by librosa

# Audio backends (at least one required for soundfile)
cffi>=1.15.0               # For soundfile backend

# Note: For GPU support, install PyTorch with CUDA:
# pip install torch --index-url https://download.pytorch.org/whl/cu121
